<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Cooker.Pages.SettingsPage"
    BackgroundColor="{DynamicResource PageBackgroundColor}">

    <VerticalStackLayout
        Padding="20"
        Spacing="15">

        <Label
            Text="Settings"
            FontSize="28"
            FontAttributes="Bold"
            TextColor="{DynamicResource PrimaryTextColor}"/>

        <HorizontalStackLayout>

            <Label 
                Text="Notifications"
                TextColor="{DynamicResource PrimaryTextColor}"/>

            <Switch
                x:Name="NotificationSwitch"
                Toggled="NotificationChanged"/>

        </HorizontalStackLayout>

        <HorizontalStackLayout>

            <Label 
                Text="Timers Vibration"
                TextColor="{DynamicResource PrimaryTextColor}"/>
            <Switch
                x:Name="VibrationSwitch"
                Toggled="VibrationChanged"/>

        </HorizontalStackLayout>

        <HorizontalStackLayout>

            <Label
                Text="Appearance"
                FontSize="18"
                FontAttributes="Bold" 
                TextColor="{DynamicResource PrimaryTextColor}"/>

            <Picker
                x:Name="ThemePicker"
                Title="Choose Theme"
                SelectedIndexChanged="ThemePicker_SelectedIndexChanged"
                TextColor="{DynamicResource PrimaryTextColor}">

                <Picker.Items>
                    <x:String>Light</x:String>
                    <x:String>Dark</x:String>
                    <x:String>custom</x:String>
                </Picker.Items>
                
            </Picker>
            
            <Button Text="Choose Background Image"
                Clicked="ChooseBackground_Clicked"
                IsVisible="True" 
                TextColor="{DynamicResource PrimaryTextColor}"/>
            

        </HorizontalStackLayout>

    </VerticalStackLayout>

</ContentPage>